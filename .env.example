# Demo mode is used automatically when these values are absent.
# For production, create a Supabase project, apply supabase/migrations/001_initial_workspace.sql,
# then set these values in Vercel for Production/Preview environments.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
