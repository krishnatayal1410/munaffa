# Production persistence. Do not expose this value to the browser.
MONGODB_URI=
MONGODB_DB=munaffa

# Public canonical URL used for metadata and redirects.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional email/provider settings can be added when a production auth/email provider is selected.
